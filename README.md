# PR images for the Blizzard UI manifest drift branch

Renders of `wow-sim --no-addons --no-saved-vars screenshot --width 1600 --height 1200` against the 12.1.0.69497 Blizzard UI cache, scaled to 1280 px per panel.

- `master-vs-branch-1600-retail.webp`: upstream master `bbd591fe4` (left) against the branch (right), `client-retail`.
- `master-vs-branch-1600-ptr.webp`: the same pair under `client-ptr`.
- `branch-point-vs-branch-1600-ptr.webp`: the branch point `634bb762c` (left) against the branch (right), `client-ptr`.

This branch holds only images; it is not meant to be merged.
