# Mail

> Not yet implemented. These functions are planned for a future admin API addition.

| Function | Description |
|----------|-------------|
| `A_Admin.AddMail(...)` | Add a mail item to the inbox |
| `A_Admin.ClearInbox()` | Remove all mail from the inbox |
| `A_Admin.SetInboxCount(n)` | Set the number of unread mail items |
