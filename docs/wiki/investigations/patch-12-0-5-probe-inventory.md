# Patch 12.0.5 Probe Inventory

Itemized probe subfinding register for the 12.0.5 audit. Documented source states are preserved in the raw source register; machine statuses below reflect current repository evidence.

## Content

- **Source:** `data/patch-api/sources/12.0.5-probes.json`
- **Source SHA-256:** `ba32c953e6232d408eea328fc7bfb77a33a1fff803e310aefa6a76512178b344`
- **Target:** retail build `12.0.5`
- **Rows:** 38 total — 0 implemented, 35 best-effort, 0 exception-requested, 3 untriaged

| Symbol | Machine Status | Documented Status | Category | Direction | Detail |
|---|---|---|---|---|---|
| `AnimScriptProbe.HandlerMatrix` | best-effort | resolved | script support | changed | Frame, AnimationGroup, and all nine Animation subtypes accept the documented handler matrix; HasScript and SetScript agree. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `AttributeDispatchProbe.ScalarRepeat` | best-effort | resolved | attributes | changed | Repeated scalar SetAttribute dispatches OnAttributeChanged; explicit false is preserved and delivered as false. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `AttributeDispatchProbe.PanelPulse` | best-effort | best-effort | panel lifecycle | changed | Repeated ShowUIPanel pulse followed by CloseAllWindows preserves the expected panel-stack behavior. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.ForbiddenState` | best-effort | resolved | forbidden frames | changed | Normal addon frames remain un-forbidden; SetForbidden does not create forbidden state. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.ForbiddenConstructor` | best-effort | resolved | forbidden frames | changed | CreateForbiddenFrame availability and EnumerateFrames behavior are captured. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.UnitEventFilter` | best-effort | resolved | events | changed | Valid RegisterUnitEvent registration works; invalid unit filters are dropped while the event remains registered. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.AttributeWildcardFalse` | best-effort | resolved | attributes | changed | Wildcard GetAttribute preserves explicit stored false. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.AttributeWildcardValues` | best-effort | resolved | attributes | changed | Wildcard true/string values and one-, two-, and three-argument lookup behavior are preserved. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.RaiseLowerLevel` | best-effort | resolved | frame ordering | changed | Raise/Lower affect same-level ties but cannot overtake a higher frame level. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `CoreBehaviorProbe.MouseFocusOrder` | best-effort | best-effort | mouse focus | changed | GetMouseFoci/GetMouseFocus shape and hit ordering after Raise/Lower. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `DevToolsDumpProbe.FrameArrayDump` | best-effort | resolved | frame identity | changed | tinsert(frame, foo), frame slot contents, and DevTools_Dump output metadata. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `FrameIdentityProbe.IdentitySlot` | best-effort | resolved | identity | changed | Frame slot [0] contains the identity userdata token. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `FrameIdentityProbe.SurrogateDispatch` | best-effort | resolved | identity | changed | Replacing [0] redirects protection and method dispatch; [1] alone does not. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `FrameIdentityProbe.DuplicateFreshness` | best-effort | resolved | identity | changed | Duplicate named frames receive fresh Lua objects, identity tokens, and custom-field state. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `HookScriptBindingProbe.IndexedHooks` | best-effort | resolved | scripts | changed | HookScript accepts indices 0, 1, and 2; GetScript retrieves indexed hooks. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `IsProtectedProbe.LegacySetters` | best-effort | resolved | protection | changed | Legacy Protect and SetProtected methods are absent and calls fail. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `IsProtectedProbe.SecureTemplate` | best-effort | resolved | protection | changed | Secure-template buttons report protected state while ordinary frames do not. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `IsProtectedProbe.DescendantAnchorPropagation` | best-effort | best-effort | protection | changed | Child, grandchild, and protected-anchor return values are captured. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `JustifyProbe.FrameFontStrings` | best-effort | resolved | FontString layout | changed | Direct unanchored frame-layer FontStrings receive the observed default anchors and justification. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `JustifyProbe.ButtonText` | best-effort | resolved | FontString layout | changed | Implicit ButtonText FontString behavior matches the probe matrix. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `JustifyProbe.SizeVariants` | best-effort | resolved | FontString layout | changed | No-size, width-only, height-only, and width+height variants are captured. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `JustifyProbe.ExplicitAnchors` | best-effort | resolved | FontString layout | changed | TOP/BOTTOM/LEFT/RIGHT/TOPLEFT controls distinguish missing from partial anchoring. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `JustifyProbe.EditBoxRegions` | best-effort | resolved | FontString layout | changed | EditBox FontStrings, including sized and inset variants, are captured. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `JustifyProbe.MessageRegions` | best-effort | resolved | FontString layout | changed | MessageFrame and ScrollingMessageFrame owner/region behavior and TextInsets effects. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `ProtectedRetailProbe.PlainFrame` | best-effort | resolved | protection | changed | Plain frame protection/forbidden state and legacy setter behavior. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `ProtectedRetailProbe.XmlProtected` | best-effort | resolved | protection | changed | XML protected=true frame state and setters. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `ProtectedRetailProbe.SecureStore` | best-effort | best-effort | protection | changed | Secure-template child state and Blizzard Store frame observations. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `ScaleEventProbe.OrderedEvents` | best-effort | resolved | scale events | changed | DISPLAY_SIZE_CHANGED, UI_SCALE_CHANGED, and relevant CVAR_UPDATE ordering. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `ScaleEventProbe.SameSizeDuplicatePair` | untriaged | unresolved | scale events | changed | Pending occurrence-level evidence and classification for the 12.0.5 probe subfinding. |
| `SetAtlasProbe.InvalidArguments` | best-effort | resolved | texture atlas | changed | nil, no-argument, boolean, numeric, empty, and unknown atlas inputs. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `TextureSetTextureProbe.PathFdid` | best-effort | resolved | texture | changed | UI-Panel-Button-Up path assignment and retained FDID 130828. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `TextureSetTextureProbe.Clear` | best-effort | resolved | texture | changed | SetTexture(nil) and no-argument clearing behavior. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `XmlFrameLevelProbe.BareAndFixed` | best-effort | resolved | XML frame level | changed | Bare frameLevel versus fixedFrameLevel=true semantics. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `XmlFrameLevelProbe.ParentReparent` | best-effort | resolved | XML frame level | changed | Parent-level changes, unfixed-child propagation, and Lua SetFrameLevel reparenting. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `XmlFrameLevelProbe.Flags` | best-effort | resolved | XML frame level | changed | HasFixedFrameLevel and IsUsingParentLevel observations. Documented best-effort: checked-in probe source is retained, but raw SavedVariables capture is unavailable. |
| `XmlFrameLevelProbe.RawCaptureProvenance` | best-effort | unresolved | provenance | changed | Pending occurrence-level evidence and classification for the 12.0.5 probe subfinding. |
| `StoreForbiddenProbe.DropdownPopulation` | untriaged | unresolved | Store lifecycle | changed | Pending occurrence-level evidence and classification for the 12.0.5 probe subfinding. |
| `StoreForbiddenProbe.ForbiddenDescendants` | untriaged | unresolved | Store lifecycle | changed | Pending occurrence-level evidence and classification for the 12.0.5 probe subfinding. |
